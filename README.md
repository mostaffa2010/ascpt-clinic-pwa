# مركز الإسكندرية التخصصي للعلاج الطبيعي(ASCPT)
# Alexandria Specialized Center for Physical Therapy

نظام إدارة مركز الإسكندرية التخصصي للعلاج الطبيعي
- د. حسني أحمد الجويلي (إستشاري العلاج الطبيعي والتقويم الحركي للعمود الفقري)
- **العنوان:** ١٧ شارع حسين شيرين - لوران - الإسكندرية
- **تليفون المركز:** 03-5702356

## المعمارية الهندسية والإنتاجية (Production Architecture)

- **نمط النظام:** Single-Tenant Architecture (مشروع Firebase مستقل تماماً وخاص بمركز ASCPT).
- **قاعدة البيانات السحابية:** Cloud Firestore بنظام المجموعات المباشرة:
  - `patients/{patientId}`: ملفات المرضى والشيتات الطبية السريرية.
  - `sessions/{sessionId}`: تسجيل حضور الجلسات وتوريد المبالغ ونسب التحمل.
  - `expenses/{expenseId}`: مصروفات المركز التشغيلية.
  - `users/{uid}`: حسابات وبيانات الطاقم الطبي والإداري.
  - `audit_logs/{logId}`: سجل رقابي وتدقيق متكامل (Hybrid Audit Architecture):
    - العمليات الإدارية الحساسة (إنشاء، حذف، تغيير كلمة السر، وتفعيل/تعطيل الموظفين) تُسجل موثقة من جهة الخادم عبر Firebase Admin SDK (`/api/admin/users`).
    - العمليات التشغيلية (إضافة/تعديل/حذف المرضى، الجلسات، المصروفات) تُسجل بنمط Append-Only محمي من جهة العميل مع تحقق قواعد Firestore من تطابق هوية الكاتب (`userId == request.auth.uid`) وحظر أي تعديل أو حذف نهائياً.
- **الأمان والصلاحيات (Security & RBAC):**
  - اعتماد قواعد أمان صارمة في Cloud Firestore Security Rules بنمط Fail-Closed لمنع أي وصول غير مصرح به.
  - إدارة الحسابات وإضافة وتعديل وحذف الموظفين تتم حصرياً عبر نقطة نهاية خادم محمية (`/api/admin/users`) بواسطة Firebase Admin SDK.
- **العمل دون اتصال (Offline-First Resilience):**
  - تفعيل التخزين المؤقت المحلي متعدد النوافذ عبر Cloud Firestore IndexedDB Persistent Cache.
- **نظام التصدير والطباعة:**
  - محرك تصدير الإكسيل المعتمد لتقارير الإيرادات اليومية والشهرية بدقة وتصنيف كامل.
- **نظام التصميم الموحد للموبايل (Mobile Design System Tokens):**
  - اعتماد أبعاد قياسية للأزرار والكبسولات (`30px` / `38px` / `44px`)، وحظر تثبيت أزرار عائمة عشوائية فوق شريط التنقل السفلي.
  - تطبيق نظام الهوية البصرية للجنس الطبي (Clinical Gender Theming) بتدرجات زرقاء للذكور ووردية للإناث.
  - إلغاء الحاويات المتداخلة المزدوجة (Card-in-Card) وتوفير تجربة إدخال خاطفة (Fast-Track Check-in).
- **منظومة الاختبارات الآلية للحسابات الحساسة (Automated Test Suite):**
  - تشغيل اختبارات برمجية دورية عبر `npm test` للتأكد الدائم من صحة معادلات رصيد التأمين، والمطابقة المالية اليومية للخزينة، وحسابات المطالبات بدقة 100%.

### تحديثات الإصدار v2.0.2 (Firestore Free-Tier Optimization & Real-Time Sync):
- **الاستماع اللحظي التلقائي (Real-Time onSnapshot):** ربط جلسات ومواعيد اليوم بمستمعات سحابية مباشرة للتحديث الفوري عبر كل أجهزة المركز (استقبال/أطباء) بأقل تكلفة قراءات (قراءة واحدة للمستند الجديد وصفية للحذف).
- **إلغاء الاستعلامات المتكررة (Zero N+1 Queries):** الاعتماد المباشر على بيانات الجلسات المسجلة ورقم الجلسة المخزن مسبقاً بدلاً من تكرار استعلامات تاريخ المريض في حلقات متكررة.
- **تأمين حدود الاستعلامات (Strict Scoped Queries):** حظر استدعاءات الجلسات المفتوحة في شاشة المطالبات وحصرها دائماً في النطاق الزمني المحدد أو الشهر الحالي.
- **التحميل الكسول الذكي (Lazy Loading):** منع جلب بيانات الحسابات والمطالبات والمواعيد دفعة واحدة في الخلفية عند فتح التطبيق، وقصر التحميل على الشاشة النشطة فقط.
- **تثبيت الخيارات السريرية والشركات محلياً:** حفظ خيارات المركز وشركات التأمين في التخزين المحلي الدائم، وتخطي 7 قراءات سحابية في كل عملية إعادة تحميل.

### تحديثات الإصدار v2.0.3 (Friday Holiday Exclusion & Smart Appointment Copy/Repeat):
- **تحديد يوم الجمعة كعطلة رسمية بالمركز:** استبعاد يوم الجمعة بالكامل من شريط أيام الأسبوع في جدول المواعيد (6 أيام عمل فقط: السبت إلى الخميس)، وعرض بطاقة عطلة أنيقة عند اختيار الجمعة، وتخطي يوم الجمعة تلقائياً عند التنقل بين الأيام، ومنع الحجز فيه.
- **ميزة نسخ وتكرار المواعيد الذكية (Smart Appointment Copy/Repeat):** إتاحة زر نسخ فوري على بطاقة الموعد وداخل نافذة الإجراءات، مع نافذة مخصصة تتضمن أزرار تثبيت شيفتات المركز السريعة (سبت • اثنين • أربعاء) أو (أحد • ثلاثاء • خميس)، وتكرار الموعد بنقرة واحدة بنفس الطبيب والفترة مع منع التكرار المزدوج.

### تحديثات الإصدار v2.0.4:
- إصلاح فوري لقوس إغلاق كلاس AppointmentsManager ومنع توقف تشغيل الواجهة.
- تأمين دورة رسم لوحة الطبيب (Doctor Dashboard re-entry guard) ومنع التداخل أثناء المزامنة اللحظية.

### تحديثات الإصدار v2.0.5 (Clinical Programs & Senior/Junior Doctor Tiers):
- **نظام البرامج السريرية المتخصصة:** إلغاء خانة الاختيار العامة وتصنيف الجلسات إلى: عام/عظام (Regular)، اعوجاج عمود فقري (Scoliosis)، شلل وأعصاب (Hemiplegia)، وتأهيل أطفال (Pediatric).
- **التعيين التلقائي الذكي من ملف المريض:** تحديد فئة الحالة في ملف المريض وتوريثها تلقائياً عند تسجيل حضور الجلسة بنقرة واحدة.
- **منظومة رتب الأطباء (Senior vs Junior Tiers):** دعم مستويات الخبرة في بروفايل الطبيب مع قوالب أسعار افتراضية جاهزة وتعديل مخصص لكل برنامج.
- **تحديث لوحة الطبيب والحسابات المالية:** تفنيط مستحقات الأطباء طبقاً لكل برنامج علاجي بنظام شفاف ودقيق.

### تحديثات الإصدار v2.0.6:
- إزالة أي أسعار افتراضية مفروضة مسبقاً في الواجهة، وضمان بقاء الحقول فارغة (0) ما لم تحددها إدارة المركز يدوياً.
- توحيد وموازنة كروت طاقم العمل (Staff Stack Cards) في لوحة الإدارة؛ بحيث تتطابق كروت المدير والسكرتارية مع كروت الأطباء في الارتفاع والأبعاد لمنع أي تفاوت بصري أثناء التمرير والسحب.

### تحديثات الإصدار v2.0.7:
- تحويل كروت الأطباء وطاقم العمل في لوحة تحكم المدير على اللابتوب والشاشات الكبيرة إلى شبكة كروت متجاورة (Responsive Cards Grid) مثل شاشة الجلسات تماماً، وإلغاء نظام السحب المتراكم (Swipe Deck) على الشاشات الكبيرة مع إبقائه سلساً على الموبايل فقط.
- تطبيق نفس البنية لشبكة كروت المواعيد على الشاشات الكبيرة بحيث تُعرض جميع فترات ومواعيد اليوم جنباً إلى جنب دون الحاجة للتمرير أو السحب، مع إخفاء أزرار التنقل السفلية على الديسكتوب.
- حذف سكشن "بيان تفصيلي بمرضى اليوم" من سجل التقرير اليومي بشاشة الحسابات وتنظيف كافة الأكواد ومستمعات الأحداث التابعة له لمنع التكرار مع شاشة الجلسات.

### تحديثات الإصدار v2.0.8:
- استبدال برنامج تأهيل الأطفال بالكامل ليصبح Quadriplegia (شلل رباعي) في كافة شاشات ونماذج النظام:
  - نموذج تسجيل الجلسات (Session Registration Form)
  - الشيت الطبي ونموذج المريض (Patient Clinical Sheet)
  - لوحة الأطباء واحتساب الأتعاب (Doctor Dashboard & Program Dues)
  - إعدادات الأطباء والشفتات وحقول الأجور بالإدارة (Admin Doctor Rates & Shift Modal)
  - مع الحفاظ الكامل على التوافق الرجعي لأي جلسات سابقة مسجلة.

### تحديثات الإصدار v2.0.9:
- إصلاح خطأ برمجي طارئ (ReferenceError: programBadge is not defined) في دالة عرض سجل المرضى `renderPatients`؛ مما كان يوقف رندر الجدول وكروت المرضى في الواجهة ويترك الشاشة في وضعها الافتراضي الفارغ بالرغم من تحميل المرضى في العداد.
- تم تعريف شارة البرنامج العلاجي لكل مريض بدقة تامة وإعادة ظهور جميع المرضى في الجدول وكروت الموبايل بصورة فورية وسلسة.

### تحديثات الإصدار v2.1.0:
- تحويل أزرار اختيار البرنامج العلاجي للجلسة (عام/عظام، Scoliosis، Hemiplegia، Quadriplegia) إلى شبكة رباعية متناسقة 2×2 (2 Columns x 2 Rows) بدلاً من الصف الواحد المضغوط؛ لتوفير مساحة لمس مريحة ومظهر بصري فخم على الموبايل وكافة الشاشات.

### تحديثات الإصدار v2.1.1:
- حل مشكلة الخروج المفاجئ والتحويل إلى الصفحة الرئيسية أثناء طباعة مستندات المريض (إفادة طبية، خطاب تجديد التأمين، إيصال استلام النقدية، الشيت الطبي).
- تأمين دورة حياة أمر الطباعة لمنع التقاط أحداث popstate وتصفير الانتقال القسري للرئيسية أثناء تفاعل نظام الطباعة في أندرويد.
- إضافة مهلة استقرار للواجهة (Layout Reflow Stabilization) لضمان جهوزية القالب المطبوع وفتحه بسلاسة في معاينة الطباعة مع الحفاظ التام والكامل على تصميم الأوراق والمستندات دون أي تغيير.

### تحديثات الإصدار v2.1.2:
- إصلاح خطأ برمجي حرج في تهيئة متغير حالة الطباعة في بداية ملف `app.js` ومنع توقف إقلاع التطبيق بالكامل عند التشغيل.
- إزالة قفل الطباعة المسبق من شاشة المرضى وربطه حصرياً بدورة حياة `window.print` العامة لضمان استدعاء نافذة الطباعة دون أي تعليق أو منع خاطئ.
- تحسين مسار الرجوع الافتراضي في سجل المتصفح (`popstate`) لمنع أي تجميد أثناء التنقل.

### تحديثات الإصدار v2.1.3 (Firestore Runaway Reads Optimization):
- **معالجة استهلاك قراءات Firestore المرتفعة:** استبدال المستمعات اللحظية المفتوحة لكامل مجموعات المرضى (`patients`) والمواعيد (`appointments`) بنمط مستند الإصدار الخفيف (`meta/syncVersion`).
- **تحويل تكلفة المزامنة اللحظية من O(N) إلى O(1):** يستمع كل جهاز نشط الآن إلى مستند واحد فقط (`meta/syncVersion`) بكلفة قراءة واحدة فقط لكل تحديث فعلي، بدلاً من إعادة جلب كافة مستندات المجموعة عند كل إعادة تحميل للتبويب أو اهتزاز في اتصال الشبكة.
- **زيادة الإصدار الذري عند التعديل:** تحديث تلقائي للحقلين `patientsVersion` و `appointmentsVersion` عبر الدالة الذرية `increment(1)` عند إضافة أو تعديل أو حذف أي مريض أو موعد.
- **الحفاظ التام على المزامنة الحية وعمل الأوفلاين:** انعكاس التغييرات فورياً بين كافة أجهزة المركز، مع الاعتماد التام على التخزين المحلي المؤقت (IndexedDB / LocalStorage) لحماية العمل دون اتصال.
- **تحديث قواعد أمان Firestore:** إضافة تصريح القراءة والكتابة على `/meta/syncVersion` لكافة أفراد طاقم العمل النشطين والمعتمدين (`isUserActive()`).
