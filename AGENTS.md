# AGENTS.md — قواعد العمل على مشروع ASCPT Clinic PWA

ملف قواعد ثابت لأي وكيل ذكاء اصطناعي (Antigravity، Spark، أو أي وكيل تاني) 
يشتغل على المشروع ده. اقرأه بالكامل قبل أي تعديل، أياً كان حجمه.

مالك المشروع (مصطفى) **لا يقرأ كود**. القواعد دي مبنية على تجربة فعلية طويلة 
(رحلة إزالة style.css وتوحيد design tokens)، ومكسورة من غلطات حقيقية حصلت 
قبل كده. الالتزام بيها مش اختياري.

---

## 1. Workflow الأساسي: Branch → موافقة → PR

- **ممنوع أي تعديل مباشر على `main` نهائياً.**
- كل مهمة تبدأ بعمل branch جديد من `main` (اسم واضح يوصف المهمة).
- كل التعديلات تتعمل على الـ branch ده فقط.
- **إنشاء الفرع محلياً وعن بُعد (Locally & Remotely):** عند فتح أي فرع جديد لأي مهمة، يتم إنشاؤه محلياً ورفعه فوراً إلى الـ Remote (`git push -u origin <branch-name>`)، حتى يتمكن مصطفى من تجربة ومعاينة التعديلات حية عبر Vercel Preview قبل إعطاء الموافقة والاعتماد.
- بعد كل دفعة تعديل، يتم دفع التغييرات للفرع البعيد، ثم التوقف وعرض النتيجة والاختبار على مصطفى لتجربتها على Vercel.
- لا تفتح Pull Request إلا بعد موافقة صريحة منه على كل حاجة اتعملت وتجربتها بنجاح.
- **`main` هو الفرع الوحيد الدائم:** ممنوع بقاء أي branch فرعي محلياً أو عن بُعد (`locally or remotely`) بعد الانتهاء من مهمته؛ وإذا وُجد أي فرع آخر غير `main` (بخلاف فرع المهمة الجارية)، يُحذف فوراً بدون الرجوع لمصطفى.
- بعد الـ merge، يُحذف الـ branch محلياً وعن بُعد فوراً.
- ممنوع الدمج التلقائي (auto-merge) تحت أي ظرف.


---

## 2. قاعدة الحذف الصارمة (Zero-Assumption Deletion)

- **أي كلاس، ID، selector فرعي، دالة، أو سطر كود — لا يُحذف إلا لو كان اسمه 
  مكتوب صراحة في الموافقة المعتمدة من مصطفى.**
- لو عنصر شكله "تابع" أو "مرتبط" بحاجة معتمدة للحذف (زي selector فرعي 
  `.parent .child`)، **وقف واعرضه بالاسم الصريح** قبل ما تلمسه. الافتراض 
  ممنوع حتى لو المنطق يبدو بديهي.
- قبل الحكم على أي كود إنه "ميت" أو غير مستخدم:
  1. ابحث نصياً في كل ملفات HTML/JS (بحث كامل + بحث عن بادئات لاحتمال بناء 
     الاسم ديناميكياً)
  2. ابحث في تاريخ Git الكامل (`git log -S "اسم_العنصر"`) لمعرفة سياقه
  3. افحص لو مرتبط بحالة نادرة (دور مستخدم معين، feature flag، قالب طباعة 
     مش شائع الاستخدام)
  4. لو فيه أي شك، صنّفه "غامض" واعرضه لمصطفى بدل ما تقرر لوحدك

---

## 3. الدفعات الصغيرة والاختبار (Small Batches + Test Between)

- ممنوع تنفيذ أكتر من ملف واحد أو مكوّن واحد في نفس الوقت، إلا لو الحجم صغير 
  جداً وواضح الأمان.
- بعد كل دفعة، اختبار فعلي (مش افتراضي) قبل الانتقال للدفعة اللي بعدها.
- لأي تعديل في ملفات مرتبطة بالطباعة (`print.css` أو أي عنصر له أثر فيه)، 
  اختبار معاينة طباعة فعلي قبل وبعد التعديل، مش بس فحص نصي.
- نسخة احتياطية / commit محلي قبل أي حذف نهائي.

---

## 4. تحليل الأثر على الكود المشترك (Shared Code Impact Analysis)

قبل تعديل أي دالة، كلاس، أو مكوّن مستخدم من أكتر من مكان واحد في المشروع 
(دوال `db.js`، `app.js`، كلاسات CSS مشتركة، `utils.js`، إلخ):

1. ابحث بحث شامل في المشروع كله عن كل أماكن استخدامه
2. التقرير النهائي لازم يتضمن قايمة اختبار يدوي (manual-test checklist) 
   مبنية على نتيجة البحث ده تحديداً، مش قايمة عامة

---

## 5. حماية بيانات المرضى الحقيقية (Data Safety Gate)

هذا التطبيق فيه بيانات مرضى حقيقية. **أي تغيير يمس البيانات المخزّنة فعلياً 
(مش الكود بس) — حذف حقل، تغيير بنية مستند، migration، كتابة جماعية (bulk 
write)، أو أي حاجة ممكن تغيّر أو تمسح بيانات مريض/جلسة/موعد موجودة — 
يتطلب توقف وتأكيد صريح من مصطفى قبل التنفيذ، بدون استثناء.**

هذا منفصل تماماً عن قواعد حذف الكود في القسم 2 — هنا بنتكلم عن البيانات 
نفسها، مش الكود اللي بيتعامل معاها.

---

## 6. حدود استهلاك Firestore

- المشروع على خطة Firebase Spark المجانية، وسقفها الفعلي 50,000 قراءة/يوم 
  (Spark plan بدون بطاقة دفع مربوطة — تخطي السقف يوقف القراءات الجديدة 
  لحد نص الليل بتوقيت المحيط الهادي، مفيش أي تكلفة مالية في أي سيناريو).
- **الهدف التشغيلي المطلوب: ~10,000 قراءة/يوم**، يعني هامش أمان 5 أضعاف 
  تحت السقف الفعلي، مخصص عشان يمتص أي طفرة غير متوقعة من غير ما التطبيق 
  يتعطل فجأة.
- المبدأ الأساسي: **فتح التطبيق العادي ميجيبش كل البيانات من القاعدة في 
  كل مرة.** الاعتماد الأساسي على `localStorage` و`IndexedDB` كـcache محلي، 
  والقراءة من Firestore بس لما يكون فيه داعي حقيقي (بيانات جديدة، تغيير 
  فعلي).
- **قاعدة صارمة لأي query أو listener جديد (`onSnapshot`, `.get()`):**
  - ممنوع query بدون `.limit()` على أي collection ممكن يكبر حجمها مع 
    الوقت (مرضى، جلسات، مواعيد)، إلا لو فيه سبب موثّق ليه
  - ممنوع `onSnapshot` على collection كاملة من غير query مقيد (`where`) — 
    أي listener لازم يكون محدود النطاق (مريض واحد، يوم واحد، شهر واحد)، 
    مش كل الداتا
  - قبل إضافة أي query/listener جديد، لازم تقدير تقريبي لعدد القراءات 
    المتوقعة منه في اليوم (حتى لو تقريبي)، ويتذكر في التقرير النهائي 
    للمهمة
- كل تعديل بيضيف أو يغيّر query/listener، افحص الأول: هل ممكن الاعتماد 
  على الكاش المحلي بدل قراءة جديدة؟
- من وقت لآخر، افحص الأنماط الحالية للقراءة مع زيادة حجم البيانات (عدد 
  المرضى/الجلسات)، وأبلغ لو حسّيت إن الاستهلاك بيقرب من الهدف (10,000).
- **مصطفى منصوح يتابع تبويب "Usage" في Firebase Console بنفسه بشكل دوري** 
  (مرة أسبوعياً كافية) كمرجع حقيقي مستقل عن أي تقدير من الوكيل — الكود 
  مقدرش يضمن الاستهلاك الفعلي 100%.

---

## 7. عزل التعديل (Change Isolation)

- المهمة تتنفذ بالظبط زي ما اتطلبت، **بدون أي تعديل إضافي "وأنا موجود هنا"**، 
  حتى لو التعديل شكله تحسين بديهي.
- أي تحسين إضافي تلاحظه أثناء الشغل، **اذكره في تقريرك كاقتراح منفصل**، 
  ولا تنفذه من غير طلب صريح.
- هذا مهم بشكل خاص لأن مصطفى مش بيقرا كود، ومش هيلاحظ تعديل غير مطلوب 
  إلا لو ظهر أثره بصرياً أو وظيفياً.

---

## 8. معايير جودة الكود (Code Quality Baseline)

- تعقيم كل البيانات المعروضة عبر `escapeHTML()` لمنع ثغرات XSS (قاعدة 
  ثابتة في المشروع من الـReadme).
- صفر `!important` في أي كود CSS جديد (إلا لو ضروري فعلاً وموثّق ليه).
- ممنوع أي `style="..."` inline جديد.
- ممنوع ترك كود تصحيح (`console.log` وما شابه) في التسليم النهائي.
- أي منطق مهم أو خطر (حسابات مالية، مزامنة بيانات) يستاهل تغطية اختبار.
- عناصر تفاعلية جديدة (أزرار، حقول) لازم تاخد سمات إتاحة (accessibility) 
  مناسبة.

---

## 9. تطوير الميزات وإصلاح الأخطاء

- أي ميزة جديدة لازم تحافظ على البنية والمعمارية الحالية (فصل الشاشات في 
  `css/views/`، المكونات في `css/components/`، الاعتماد على design tokens).
- أي إصلاح خطأ: تشخيص السبب الجذري الأول، تعديل بأقل قدر ممكن من التغيير، 
  حماية من رجوع نفس الخطأ (regression)، واختبار فعلي حقيقي — مش افتراض 
  إن الإصلاح شغال.

---

## 10. التقرير النهائي (مختصر، مش تقرير منفصل مكلف)

في نهاية أي مهمة، **سطرين إلى ثلاثة بس** بلغة بسيطة (مش تقنية) يوضحوا:
- إيه اللي اتعمل فعلياً
- إيه اللي اتجرب وطلعت نتيجته إيه
- أي حاجة محتاجة قرار أو موافقة من مصطفى

ممنوع تقرير مفصّل طويل أو خطوة "تحليل" منفصلة لمجرد كتابة التقرير — 
الهدف وضوح سريع بأقل تكلفة، مش توثيق شامل.

---

## 11. مرجع بنية المشروع الحالية

- `css/design-tokens.css` — كل الألوان والـradius والظلال والخطوط 
  (كمتغيرات CSS)، مصدر الحقيقة الوحيد لأي قيمة تصميم
- `css/base.css` — إعادة ضبط عامة وقواعد أساسية
- `css/components/*.css` — مكونات مشتركة (أزرار، كروت، بادجات، فورمات، 
  مودالز، skeletons، جداول)
- `css/views/*.css` — كود كل شاشة لوحدها (shell, reception, patients, 
  doctor, sessions, admin, finance, claims)
- `css/print.css` — قوالب الطباعة، **حساس جداً**، أي لمس ليه يتطلب اختبار 
  طباعة فعلي قبل وبعد
- `docs/DOM_COUPLING_MAP.md` — قايمة العناصر المحمية اللي مربوطة بـJS، 
  فيها حراسة تلقائية في `tests/ui-components.test.js`
- الاعتماد على `localStorage`/`IndexedDB` كـcache أساسي، Firestore للقراءة 
  الفعلية بس عند الحاجة

---

## 12. كفاءة السياق والشبكة (Context & Network Efficiency)

- **حظر استدعاء الوكلاء الفرعيين (Subagents Strictly Forbidden):** يُمنع تماماً إنشاء أو استدعاء أي وكلاء فرعيين (`subagents`). التنفيذ أحادي الخيط فقط (`single-threaded execution`).
- **منع المسح التكراري للمستودع (No Recursive Scanning):** ممنوع منعاً باتاً عمل مسح شامل أو تكراري لشجرة المستودع بالكامل (`strictly forbid recursive repository scanning`).
- **حصر الفحص بالملفات المحددة (@-scoped files only):** يُحظر فحص أو فتح أو تعديل أي ملف خارج النطاق المحدد صراحة في المهمة (@-scoped files).
- **حظر فحص المجلدات والملفات الثقيلة والمستبعدة:** يُحظر صراحة وبشكل قاطع مسح أو فحص مجلد `node_modules`، أو ملفات الـ build caches، أو ملفات Git الداخلية (`.git`)، أو أي ملفات متجاهلة في `.gitignore`.
- **ترشيد استهلاك السياق:** الحفاظ على كفاءة الـ tokens وتقليل استهلاك الشبكة بالتركيز فقط على الملفات المعنية بالتعديل المباشر.

